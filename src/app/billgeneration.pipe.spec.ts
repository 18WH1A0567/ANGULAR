import { BillgenerationPipe } from './billgeneration.pipe';

describe('BillgenerationPipe', () => {
  it('create an instance', () => {
    const pipe = new BillgenerationPipe();
    expect(pipe).toBeTruthy();
  });
});
